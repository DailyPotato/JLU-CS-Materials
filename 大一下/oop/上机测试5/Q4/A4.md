# A4

1. 1
2. 有,data1是类A的私有成员,无法被类B实例直接访问
3. 换成protected后,在public继承下,可以在类内访问基类的数据成员
4. 都合法,g函数执行的是类A的,其余执行的是类B的
5. b.g()不合法了,因为g()是类A的函数,现在private继承后,类B里就不能调用类A的函数了
6. 6
7. a.f();b.f();a.g();b.g();b.f(1);b.h();可以通过编译,执行结果如下:
    Excute A::f() Data1=10
    Excute B::f() Data1=1 Data2=2
    Excute A::g()
    Excute A::g()
    Excute B::f(int)  n=1 Data1=1 Data2=2
    Excute B::h()
    Destory B
    Destory A
    Destory A
8. 派生类的实例中本身就有基类的完整实例,且派生类实例的指针指向的就是基类实例的地址,所以向上类型转换是十分安全的