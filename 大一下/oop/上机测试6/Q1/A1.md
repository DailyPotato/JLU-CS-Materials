# A1

1. 有virtual时，在delete时会先调用B的析构函数，再调用A的析构函数，去掉virtual后，仅调用A的析构函数
2. 不可以，去掉的话B里的func1就不是A的func1的override，会导致B类也退化成抽象类，无法被实例化
3. 匹配的是n=500，因为默认参数是静态绑定的，在编译的时候，就已经被编译成了func2（500），结论：虚函数的调用取决于指针/引用所指对象的动态类型（运行时），但默认参数的取值完全取决于指针/引用的静态类型（编译时）
4. 不可以，会导致B类无法访问static int lineno
5. 无影响
6. 初始化静态变量
7. 不可以，A是抽象类，具有纯虚函数，不能被实例化，而B是具体类，可以被实例化
8. 会直接报错，因为只有虚函数允许=0
9. 因为A类里根本没有func3（）函数，即使把（8）改为virtual也仍然没有
10. 因为编译时，要先经过静态类型检查，而A类里的func1是无参的，无法成功匹配，所以编译报错
